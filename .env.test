# 测试环境配置

# 服务器配置
PORT=8080
NODE_ENV=test

# 数据库配置
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=password
DB_NAME=medical_platform_test
DB_DIALECT=mysql

# JWT配置 - 测试环境使用不同的密钥
JWT_SECRET=test_jwt_secret_key_for_testing_environment
REFRESH_SECRET=test_refresh_secret_key_for_testing_environment
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=1d

# CORS配置 - 测试环境限制来源
CORS_ORIGIN=http://localhost:8081,http://test.example.com

# Redis配置 - 使用提供的Redis信息，不同DB
REDIS_HOST=ivan.black
REDIS_PORT=6379
REDIS_DB=2
REDIS_PASSWORD=
REDIS_PREFIX=test_token_blacklist:

# 邮件配置
MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_USER=test@example.com
MAIL_PASSWORD=testmailpassword
MAIL_FROM=test@example.com